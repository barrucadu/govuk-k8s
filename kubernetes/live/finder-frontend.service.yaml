apiVersion: v1
kind: Service
metadata:
  name: finder-frontend
  namespace: live
spec:
  selector:
    run: finder-frontend
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
  type: NodePort
---
apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: finder-frontend
  namespace: live
  annotations:
    kubernetes.io/ingress.class: alb
    alb.ingress.kubernetes.io/scheme: internal
    alb.ingress.kubernetes.io/tags: App=finder-frontend,Namespace=live
    alb.ingress.kubernetes.io/healthcheck-path: /search
spec:
  rules:
    - host: finder-frontend.live.in-cluster.govuk-k8s.test
      http:
        paths:
          - path: /*
            backend:
              serviceName: finder-frontend
              servicePort: 3000
