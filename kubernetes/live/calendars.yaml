apiVersion: v1
kind: Service
metadata:
  name: calendars
spec:
  selector:
    run: calendars
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
      nodePort: 31002
  type: NodePort

---

apiVersion: apps/v1
kind: Deployment
metadata:
  name: calendars
spec:
  selector:
    matchLabels:
      run: calendars
  replicas: 2
  template:
    metadata:
      labels:
        run: calendars
    spec:
      containers:
        - name: calendars
          image: registry.govuk-k8s.test:5000/calendars
          env:
            - name: PLEK_SERVICE_SEARCH_URI
              value: https://www.gov.uk/api
            - name: PLEK_SERVICE_WHITEHALL_ADMIN_URI
              value: https://www.gov.uk
            - name: GOVUK_WEBSITE_ROOT
              value: https://www.gov.uk
            - name: PLEK_SERVICE_CONTENT_STORE_URI
              value: https://www.gov.uk/api
            - name: PLEK_SERVICE_STATIC_URI
              value: assets.publishing.service.gov.uk
            - name: K8S_HOSTNAMES
              value: calendars.live.web.govuk-k8s.barrucadu.co.uk
            - name: HOST
              value: 0.0.0.0
          ports:
            - containerPort: 3000
              protocol: TCP
