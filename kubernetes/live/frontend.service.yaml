apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: live
spec:
  selector:
    run: frontend
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
  type: NodePort
---
apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: frontend
  namespace: live
  annotations:
    kubernetes.io/ingress.class: alb
    alb.ingress.kubernetes.io/scheme: internal
    alb.ingress.kubernetes.io/tags: App=frontend,Namespace=live
    alb.ingress.kubernetes.io/healthcheck-path: /
spec:
  rules:
    - host: frontend.live.in-cluster.govuk-k8s.test
      http:
        paths:
          - path: /*
            backend:
              serviceName: frontend
              servicePort: 3000
